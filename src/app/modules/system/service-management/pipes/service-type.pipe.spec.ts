import { ServiceTypePipe } from './service-type.pipe';

describe('ServiceTypePipe', () => {
  it('create an instance', () => {
    const pipe = new ServiceTypePipe();
    expect(pipe).toBeTruthy();
  });
});
